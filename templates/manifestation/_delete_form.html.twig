<form method="post" action="{{ path('app_manifestation_delete', {'id': manifestation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manifestation.id) }}">
    <button class="btn">Delete</button>
</form>
