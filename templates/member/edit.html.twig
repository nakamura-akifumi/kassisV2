{% extends 'base.html.twig' %}

{% block title %}利用者編集{% endblock %}

{% block body %}
<div class="container py-3">
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
            <h1 class="h4 mb-1">利用者編集</h1>
            <p class="text-muted small mb-0">利用者情報を更新します。</p>
        </div>
        <a class="btn btn-outline-secondary btn-sm" href="{{ path('app_member_index') }}">一覧へ</a>
    </div>

    {% include 'member/_form.html.twig' with {'form': form, 'button_label': '更新'} %}

    <div class="mt-3">
        {% include 'member/_delete_form.html.twig' %}
    </div>
</div>
{% endblock %}
